import Phase from "@/components/phases/filters";

export const metadata = {
  title: "Browse & Filter Deals | WholesaleUp",
  description: "Search and filter wholesale deals by category, price, location, and more.",
};

export default function FiltersPage() {
  return <Phase />;
}
