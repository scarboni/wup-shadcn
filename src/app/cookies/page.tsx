import Phase from "@/components/phases/cookies";

export const metadata = {
  title: "Cookie Policy",
  description: "Read the WholesaleUp cookie policy.",
  alternates: { canonical: "/cookies" },
};

export default function CookiesPage() {
  return <Phase />;
}
