import Phase from "@/components/phases/shell";

export const metadata = {
  title: "Shell Layout | WholesaleUp",
  description: "Main application shell with sidebar navigation, header, search, user dropdown, and footer.",
  robots: { index: false, follow: false },
};

export default function ShellPage() { return <Phase />; }
